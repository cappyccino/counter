import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
    <React.StrictMode>
        <p>Counter is 0</p>
    </React.StrictMode>,
    document.getElementById('root')
);
